/**
 * Re-export pipeline output types from SDK.
 * These are the canonical definitions shared between Platform and Web.
 */
export type {
  CatalogSummary,
  CatalogScores,
  PipelineMeta,
  CatalogsOutput,
} from "@agora/sdk";
